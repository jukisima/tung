use ground.tung

deed pick {
  𝟙 pick: ℤ
}

let branch-sum: ℤ =
  try (only pick) + (only pick) {
    pick @ (2 eftgin) + (3 eftgin)
  }

let (_: 𝟙) main: 𝟙 ! console = (
  let _ = 'multi-shot' write-line
  yield branch-sum to-text $ write-line
)
