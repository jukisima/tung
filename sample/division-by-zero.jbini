bring prelude.jbini;

let quotient: 𝟙 → integer ! console =
  { _ ↦ try 1 ÷ 0 {
    fail ↦ (
      let _ = 'division by zero caught; using 0' write-line;
      0
    )
  } };

((null quotient) to-string) write-line
