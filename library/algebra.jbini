bone a semigroup {
  a * a: a
  # a * (b * c) ≡ (a * b) * c
}

given a semigroup bone a monoid {
  ∅: a
}

bone morphisms category {
  id: a morphisms a;
  (a morphisms b) compose (b morphisms c): a morphisms c
}
