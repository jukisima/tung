use algebra/total/magma.tung
use algebra/total/semigroup.tung
use data/two.tung
use order/lattice.tung

graiþ a semigroup
show frame a monoid {
  let ∅: a

  law (a: a): ∅ * a ~ a
  law (a: a): a * ∅ ~ a
}

graiþ b monoid
fill (a func b) monoid {
  let _ ∅ = ∅
}

fill (𝟚 infimal) monoid {
  let ∅ = yea infimal
}

graiþ a semilattice-supremal-bounded
fill (a supremal) monoid {
  let ∅ = ⟂ supremal
}
