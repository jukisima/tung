use algebra/partial/semigroupoid.tung

graiþ ⇨ semigroupoid
show frame ⇨ category {
  let identity: a ⇨ a

  law (f: a ⇨ b): identity ∘ f ~ f
  law (f: a ⇨ b): f ∘ identity ~ f
}

fill func category {
  let a identity = a
}
